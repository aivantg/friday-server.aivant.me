import type { Ask, AskResponse } from '../utils/types';

export const handleConversationNote = async (
  ask: Ask<'conversationNote'>
): Promise<AskResponse> => {
  throw new Error('Not implemented');
};
